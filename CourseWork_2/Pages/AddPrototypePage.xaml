<Page
    x:Class="CourseWork_2.Pages.AddPrototypePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CourseWork_2.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#2b3346">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border BorderThickness="0,0,0,1" BorderBrush="Gray">
            <Grid>
                <Button Background="Transparent" Content="Cancel" Foreground="White"
                        FontSize="18" Margin="10,0,0,0" FontWeight="Medium" />
                <TextBlock Text="Add Prototype" FontSize="20" FontWeight="Medium"
                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <Button Background="Transparent" Content="Create" Foreground="White" FontWeight="Medium"
                        FontSize="18" HorizontalAlignment="Right" />
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <StackPanel>
                <TextBox PlaceholderText="Url" Foreground="White" Background="Transparent"
                         Margin="0,10,0,0" BorderThickness="0,1" />
                <TextBox PlaceholderText="Name" Background="Transparent" 
                         Margin="0,10,0,0" BorderThickness="0,1" />
                <TextBox PlaceholderText="Description" Height="Auto" 
                         Background="Transparent" Margin="0,10,0,0" BorderThickness="0,1" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
